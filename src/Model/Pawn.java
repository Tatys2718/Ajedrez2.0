package Model;

import javax.swing.*;

public class Pawn extends Piece{
    public Pawn(int positionRow, int positionColumn, boolean isWhite, ImageIcon image) {
        super(positionRow, positionColumn, isWhite, image);
    }
}
